--  selectors
SELECT
    *
FROM
    employee;

SELECT
    role.id,
    role.title,
    role.salary
FROM
    role
ORDER BY
    role.id;

SELECT
    role.id,
    role.title
FROM
    role
ORDER BY
    role.id;

-- department selectors
-- role selectors
-- employee selectors